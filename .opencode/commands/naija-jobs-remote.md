---
description: Find remote jobs open to Nigeria-based candidates
---

Find remote jobs open to Nigeria-based candidates.

Load the naija-jobs skill:
```
skill({ name: "naija-jobs" })
```

Then run mode `remote` with arguments: $ARGUMENTS
