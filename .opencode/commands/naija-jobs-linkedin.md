---
description: Find the newest LinkedIn jobs (logged-out), with applicant counts
---

Find the newest LinkedIn jobs (logged-out), with applicant counts.

Load the naija-jobs skill:
```
skill({ name: "naija-jobs" })
```

Then run mode `linkedin` with arguments: $ARGUMENTS
